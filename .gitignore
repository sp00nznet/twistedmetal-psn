# Copyrighted material: the PS3 firmware emulator, the game package, and
# everything derived from them. None of this belongs in the repo.
fw/
pkg/
# extracted*/ so a second title (extracted_tm2/, extracted_tm2_crack/, ...)
# is covered without another edit -- these hold the unpacked EBOOT.PBP.
extracted*/
vfs/
*.rar
*.pkg
*.7z
*.iso
*.bin
*.PBP
*.EDAT

# Generated: lifted C++/C, capture dumps, build output.
src/recomp/
src/gen/
src/spu_gen/
analysis/functions.json
analysis/spu/
build/
scratch/
*.log
*.obj
*.map

# LD_HLSL_DUMP writes these into the CWD
hlsl_*.txt
